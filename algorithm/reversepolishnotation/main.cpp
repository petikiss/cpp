#include <string>
#include <stack>



class Tokenizer
{
  public:
   Tokenizer(std::string input) : mInput(input), mCurrentPosition(0) {}
   std::string getNextToken()
      {

      }

  private:
   std::string mInput;
   int mCurrentPosition;
};




std::string searchToken(const std::string& input)
{

}






int main()
{
   std::string input = "(icsi-ref=icsi.mmtel||mobility=fixed)&&marci!=true";

   std::stack<std::string> stack;




/*
   if (currentChar == '(')
   {
      stack.push("(");
   }
   else if(true)
   {
   }
*/


   } // for
}
